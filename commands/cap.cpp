# include "../commands.hpp"

int cap(IRCServer &server, Client &client, std::vector<std::string> &arguments)
{
	(void)server;
	(void)client;
	(void)arguments;
	//std::cout << "-->commande cap"<< std::endl;
	return (1);
}